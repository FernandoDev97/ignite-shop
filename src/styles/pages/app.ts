import { styled } from "..";

export const Container = styled('div', {
    display: 'flex',
    alignItems: 'flex-start',
    flexDirection: 'column',
    justifyContent: 'center',
    minHeight: '100vh',
})
